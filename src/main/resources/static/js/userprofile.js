(function (window, $, undefined) {

    var page;

    var jsClasses = {
        lul: ".lul",
        lul: ".lul",
    };

    $(function () {//init
        page = $(this);


        page.find(jsClasses.lul).bind("click", func);
    });

    function func() {

    }


}(window, jQuery));